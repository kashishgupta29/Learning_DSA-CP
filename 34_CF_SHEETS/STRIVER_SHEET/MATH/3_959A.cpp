#include <iostream>
#include <vector>
#include <algorithm>
#include <math.h>
using namespace std;
int main()
{
  long long n;
  cin >> n;
  if (n % 2 != 0)
    cout << "Ehab";
  else
    cout << "Mahmoud";
}