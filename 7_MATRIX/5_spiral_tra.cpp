#include <iostream>
#include <vector>
using namespace std;
void spiralPrint(vector<vector<int>> &matrix, int r, int c)
{
  int top = 0, right = r - 1, bottom = c - 1, left = 0;
  while ()
  {

  }
}
int main()
{
  int r, c;
  cin >> r >> c;
  vector<vector<int>> matrix(r, vector<int>(c));
  for (int i = 0; i < r.i++)
    for (int j = 0; j < c; j++)
      cin >> matrix[i][j];
  void spiralPrint(matrix, r, c);
}