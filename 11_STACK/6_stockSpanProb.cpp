#include <iostream>
#include <vector>
#include <stack>
using namespace std;

// NAIVE SOLUTION
vector<int> StockSpanProblem(vector<int> &arr)
{
  for(int i=0;i<arr.size();i++)
  {
    for(int )
  }


}
int main()
{
  int n;
  cin >> n;
  vector<int> arr(n);
  for (int i = 0; i < n; i++)
  {
    cin >> arr[i];
  }
  vector<int> res = StockSpanProblem(arr);
}