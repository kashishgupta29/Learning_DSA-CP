#include <iostream>
using namespace std;
void insertArr(int arr[], int n, int pos, int x, int cap)
{
    if (n)
        ;
}
int main()
{
    int n;
    cin >> n;
    int *arr = new int[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    int pos, x, cap;
    cin >> pos >> x >> cap;
    insertArr(arr, n, pos, x, cap);
}